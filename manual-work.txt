# ヌケバイ manual-work.txt (2026-08-14 作成)
#
# 【役割】本番環境での確認・操作が必要で、今すぐ完了できない「保留中タスク」を集約する。
#   - コード実装が終わっていても、本番反映(deploy)や本番データ操作、実機テストが
#     必要なものはここに記録する。
#   - 本番環境での確認ができるようになったタイミングで、このファイルを見れば
#     残タスクが分かる状態を保つ。
#   - 新たに同種のタスクが発生したら、その都度ここに追記する。
#
# ステータス: [ ] 未実施 / [x] 完了
# ============================================================================

============================================================
M-1. Phase 1〜6 の本番一括デプロイ(旧: Phase 2+3)
============================================================
状態: [x] 完了(2026-08-15 実施)✅
  実施内容:
   - Hosting: Phase 1〜6 のフロント変更を本番反映(https://nukebuy.web.app)
   - Functions: awardPointsOnPost 作成 / validateCouponV3 削除
     ※初回は Eventarc 権限伝播待ちで awardPointsOnPost 作成が一時失敗 → 数分後に再実行して成功
   - Firestore rules: firestore.rules を本番反映(構文コンパイルOK)
  → デプロイ後の本番動作確認(下記「デプロイ後の確認」)はユーザーにて実施をお願いします。

内容:
  Phase 2(セキュリティ)と Phase 3(データ整合性)の実装はコミット・push 済みだが
  本番未反映。以下は必ず「同時に」反映する(バラバラだと投稿が壊れるため)。

  理由(同時反映が必須):
   - 新ルールは users.points 等のクライアント書込を禁止する。旧フロントのままだと
     reviews.js が points をクライアント書込しようとして投稿が失敗する。
   - よって「新フロント(Hosting)」「新ルール」「ポイント付与Functions」は一括反映が必要。

  手順:
   1) npm run build
   2) firebase deploy --only hosting,firestore:rules,functions --project nukebuy
      ※functions デプロイ時、削除される関数(validateCouponV3)の確認プロンプトに注意
        (必要なら --force)。ext-firestore-stripe-payments-* は拡張管理のため影響なし。

  デプロイ後の確認(本番):
   - Web: ログイン → 報告投稿 → ポイント付与表示(15〜25pt) → 検索でヒット
   - users.points をクライアントから書き換えられないこと(投稿は正常に通ること)

  ロールバック:
   - ルールを戻す場合は firestore.rules.production-backup-2026-08-14 の内容を
     firestore.rules に戻して再デプロイ。

============================================================
M-2. Chrome拡張の実機テスト(認証・posts統一の確認)
============================================================
状態: [x] 完了(2026-08-15)✅
  確認済み: メール/パスワード・Googleログイン両方でログイン→状態保持、
    報告→「◯ポイント付与されました！」表示、Web検索で反映を確認。
  ※過程で複数の不具合を修正(F-5参照): getAuthToken→launchWebAuthFlow、
    redirect_uri表示、setPersistence await 化、付与ポイント数表示。

内容:
  Work 2-5 で拡張に認証を実装し、報告の保存先を posts に統一した。
  ※当開発環境では Chrome 拡張を実行できないため、ユーザーによる実機テストが必要。

  手順:
   1) chrome://extensions を開く → デベロッパーモード ON
   2) ヌケバイ拡張を「更新(再読み込み)」する(vendor/ 同梱・manifest 0.2.0 反映のため)
   3) 拡張ポップアップ → メール/パスワードでログイン
   4) 動画IDと理由を選んで「報告」
   5) Web 側の検索で、その動画IDが報告件数に反映されているか確認
   6) ログアウト → 未ログイン時はログインフォームが出ることを確認

  期待結果:
   - 未認証だった旧拡張(extension_reports・ルールで全拒否)から、
     認証付きで posts に保存され、Web 検索に反映される状態になる。

============================================================
M-3. videoId 正規化マイグレーション(既存 posts データ)
============================================================
状態: [x] 完了(2026-08-15)✅
  実施結果:
   - dry-run: posts 22件中、正規化が必要=1件(JeBSY5UJEPwT1Wg6bkZx:
     "MFCS-169 "(末尾空白)→ "MFCS-169")
   - ユーザー承認後 --apply 実行 → 成功1/失敗0
   - 再 dry-run で「変更が必要な doc: 0件」を確認
   - 変更は videoId フィールドのみ。報告データ(理由/投稿者/日時)は保持。

内容:
  Work 3-4。既存 posts の videoId を正規化し、過去データも検索にヒットさせる。
  変更するのは videoId フィールドのみ(他フィールドは不変=報告データは保持)。

  手順:
   1) dry-run(読み取りのみ・書込なし):
        node scripts/migrate-videoid.mjs
      → 変更が必要な件数と before→after を確認し、ユーザーへ報告
   2) ユーザー承認後に本実行:
        node scripts/migrate-videoid.mjs --apply

  注意:
   - 本番データを書き換えるため、必ず dry-run 結果の確認 → 承認 → --apply の順で行う。
   - 認証は firebase-tools のログイントークンを使用(期限切れ時は firebase login --reauth)。
   - M-1 デプロイ(新ルール適用)後に実行するのが安全(整合性のため)。

============================================================
M-4. Chrome拡張のGoogleログイン用 OAuth設定(GCP)
============================================================
状態: [x] 完了(2026-08-15)✅ Webクライアントに chromiumapp.org リダイレクトURIを
  登録し、launchWebAuthFlow による Google ログインが正常動作することを確認。
  (以下は経緯の記録)
  旧: [ ] 未完了(2026-08-15 方式変更: getAuthToken → launchWebAuthFlow)
  【経緯】当初 getAuthToken 方式で実装したが "bad client id" エラー(=クライアントが
    Chrome拡張タイプでない)。より堅牢な launchWebAuthFlow(Webアプリ型クライアント使用)
    に変更した。→ 必要な設定は「リダイレクトURIの追加」のみ。

  使用クライアントID(popup.js の GOOGLE_CLIENT_ID):
    1019921068987-ok1odk5kk0a6vgacjofn9rj1p4mdevii.apps.googleusercontent.com

  手順(ユーザーの GCP コンソール作業):
   1) GCP Console(プロジェクト nukebuy)→ APIとサービス → 認証情報
   2) 上記クライアントIDの OAuth クライアントを開く
      ※種類が「ウェブアプリケーション」であること(そうでない場合は新規に
        「ウェブアプリケーション」型を作成し、その client ID を Claude に伝える)
   3) 「承認済みのリダイレクト URI」に次を追加して保存:
        https://cenehhpmcohahgainnmpajjgfffdbjab.chromiumapp.org/
   4) 拡張を再読み込み(version 0.3.1)→「Googleでログイン」で動作確認
   5) OAuth 同意画面が「テスト中」の場合、ログインする Google アカウントを
      テストユーザーに追加

  フォールバック(もし "invalid credential" 等で弾かれる場合):
   - Firebase Console → Authentication → Sign-in method → Google の許可クライアントに
     この client ID を追加、もしくは Firebase 自動生成の Web クライアントIDを使う。
     その場合は Claude に連絡(GOOGLE_CLIENT_ID を差し替える)。

背景:
  拡張に Google ログインを実装(chrome.identity.getAuthToken + Firebase
  signInWithCredential)。動作には GCP で「Chrome拡張」タイプの OAuth クライアントIDを
  拡張IDに紐付けて作成し、manifest.json の oauth2.client_id に設定する必要がある。

  拡張ID(manifest に key を追加して固定): cenehhpmcohahgainnmpajjgfffdbjab

  ※コードは実装済み(manifest の key/identity/oauth2、popup の Google ボタン・処理)。
    現状 oauth2.client_id は "REPLACE_WITH_CHROME_EXTENSION_OAUTH_CLIENT_ID..." のプレースホルダ。

  手順(ユーザーの GCP コンソール作業):
   1) 拡張を chrome://extensions で再読み込みし、拡張IDが
      「cenehhpmcohahgainnmpajjgfffdbjab」であることを確認(key により固定)
   2) Google Cloud Console → プロジェクト「nukebuy」→ APIとサービス → 認証情報
   3) 「認証情報を作成」→「OAuth クライアント ID」
      → アプリケーションの種類:「Chrome 拡張機能」(無い場合は「Chrome アプリ」)
      → 名前: 任意(例: Nukebai Extension)
      → アプリケーション ID(項目 ID): cenehhpmcohahgainnmpajjgfffdbjab
      → 作成し、発行された「クライアント ID(...apps.googleusercontent.com)」をコピー
   4) そのクライアントIDを Claude に伝える(または manifest.json の
      oauth2.client_id を自分で置換)→ 拡張を再読み込み
   5) OAuth 同意画面が未設定/テスト中の場合、ログインするアカウントを
      テストユーザーに追加するか、公開ステータスを調整する

  完了後:
   - 拡張ポップアップの「Googleでログイン」で Google アカウントログインが可能になる。

# ============================================================================
# 以降、本番確認が必要な保留タスクが発生したらここに M-5, M-6... と追記する。
# ============================================================================
